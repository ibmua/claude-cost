AGENTS.md
CLAUDE.md
node_modules/

# Private per-machine config: extra machines, sync scripts, local log caches.
# Never commit these — they contain host names / paths for other computers.
machines.local.mjs
local/
